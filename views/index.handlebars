
<div class="container">
    <div class="row">
        <div class="col-lrg-12 text-center" id="top">
            <h1>EAT THEM UP YUM</h1>
        </div>
        <div class="row"></div>
        <div class="col-lrg-6 text-center" class="">
            <div class="row">
                {{#each burger_data}}
                    {{#unless this.devoured}}
                <div class="col-lrg-9 text-center">
                    <pre>{{this.id}}, {{this.burger_name}}</pre>
                </div>

            <div class="col-lrg-3 text-center">
                <form method="POST" action="/burgers/update?_method=PUT" class="button-size">
                    <input type="hidden" name='burger_id' type='text' value={{this.id}}>
                    <br>
                    <button type='submit' class='btn btn-default'>EAT IT UP YUM</button>
                </form>
            </div>
            {{/unless}}
            {{/each}}
            </div>
        </div>
        <div class="col-md-6 text-center" class="work">
            {{#each burger_data}}
                {{#if this.devoured}}
                    <input class"form-control" type="text" placeholder="{{this.id}}, {{this.burgername}}" readonly>
                {{/if}}
            {{/each}}
        </div>
    </div>
</div>
<div class="container">
    <div class="row">
        <div class="col-md-12 text-center enter_section">
        <form action="/burgers/create" methods="POST">
            <input type="text" name="burger_name" class="form-control" value="" id="enter_text">
            <button typer="submit" class="btn btn-primary" id="text-enter-button">submit</button>
        </form>
        <p>How you like a cook you a steak?</p>
        </div>
    </div>
</div>